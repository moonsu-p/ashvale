# 여기에 캐릭터 15장을 넣는다

「15 Top-Down Character Sprites」(piano_no_renshu, CC0)의 PNG 15장을
이 폴더 **바로 아래**에 둔다. 하위 폴더로 한 겹 더 들어가지 않게 할 것.

각 파일: 64×96, 16×24 프레임 4열×4행.
행 순서 0=아래 1=왼쪽 2=위 3=오른쪽. 상세는 `src/data/characters.ts`.

이 폴더는 팔레트 리맵에서 제외된다 (`scripts/remap-palette.ts`의 SKIP).
