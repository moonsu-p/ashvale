/**
 * 실내 서술 (§10, §15).
 *
 * **새로 쓴 파일이다.** 수정 금지 목록의 콘텐츠 파일에는 실내 문구가 없었다.
 * 그 파일들을 건드리지 않고 여기에 따로 둔다.
 *
 * 문체는 §15 를 따른다 — 방을 묘사하는 줄은 **무주어 문어체 과거형**,
 * 2인칭을 쓰지 않는다. 버튼과 항목 이름은 명사형·동사원형.
 */

import type { RoomId } from '@/types/map';

/** 방에 들어섰을 때 한 줄. 건물 레벨이 올라도 바뀌지 않는다 */
export const ROOM_INTRO: Record<RoomId, string> = {
  study:
    '필사한 종이가 벽을 채웠다. 다녀온 자들이 적어 둔 것이라 손글씨가 제각각이었다.',
  altar:
    '돌 위에 마른 꽃이 놓여 있었다. 누가 두고 갔는지는 아무도 묻지 않았다.',
  roster:
    '명부에는 이름과 함께 그 사람이 어디서 왔는지가 적혀 있었다. 지운 자국도 남아 있었다.',
  training:
    '바닥에 발자국이 겹겹이 눌려 있었다. 같은 자리를 오래 밟은 흔적이었다.',
  observatory:
    '창이 하늘 쪽으로만 났다. 밤에는 별을, 낮에는 아무것도 볼 수 없었다.',
};

/** 볼 것이 아직 없을 때. 비어 있다고 말하되 무엇을 하면 되는지 남긴다 */
export const ROOM_EMPTY: Record<RoomId, string> = {
  study: '아직 다녀온 곳이 없다. 지역에 나가면 여기 기록이 쌓인다.',
  altar: '기력이 온전하다. 바칠 이유가 없다.',
  roster: '명부가 비어 있다. 함께 걸은 사람이 생기면 여기 오른다.',
  training: '쓸 점수가 없다. 경험을 쌓아 단계가 오르면 생긴다.',
  observatory: '거둔 유물이 없다. 크게 성공한 탐사에서 이따금 나온다.',
};

/** 봉납 결과 — 연대기에 남는 줄이 아니라 그 자리에서 보이는 문구다 */
export const OFFERING_DONE = '돌 위에 금화를 얹었다. 숨이 트였다.';
export const OFFERING_POOR = '바칠 금화가 모자란다. 자원을 팔거나 탐사로 벌어 오면 된다.';
