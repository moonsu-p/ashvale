/**
 * 빌드가 심어 주는 값.
 *
 * `vite.config.ts` 의 `define` 이 컴파일할 때 문자열로 바꿔 넣는다.
 * 런타임에 존재하는 변수가 아니라 치환되는 자리라서 여기서 형만 알려 준다.
 */

/** 빌드마다 달라지는 표. 서비스 워커 캐시 이름을 가른다 */
declare const __BUILD_ID__: string;
